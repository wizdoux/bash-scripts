general {
  interval = 1
  separator = ""
  colors = false 
}

order += "battery 0"
order += "time"

battery 0 {
  format = "⚡ %percentage"
  format_percentage = "%.0f"
  last_full_capacity = true
  threshold_type=percentage 
  low_threshold=10
}

time {
  format = "⌚ %H:%M"
}

